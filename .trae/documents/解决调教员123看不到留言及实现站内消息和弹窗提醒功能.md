## 问题分析

1. **调教员123看不到留言**：
   - 留言通过CaseProgress模型存储，类型为'mediate'
   - 前端在CaseDetail.tsx的时间轴部分显示这些留言
   - 但可能存在权限或数据显示问题

2. **站内消息和弹窗提醒未实现**：
   - 前端有通知方式选项，但后端未处理
   - 前端只有简单的alert提示，不是真正的弹窗提醒
   - 后端有Socket.IO实现，但未用于消息通知

## 解决方案

### 1. 确保调教员123能看到留言

**前端修改**：
- 检查CaseDetail.tsx中留言显示逻辑，确保所有类型为'mediate'的progress都能显示
- 确保调教员能看到分配给自己的案件的所有留言

**后端修改**：
- 检查getCaseById函数，确保正确返回案件进度记录
- 确保调解员权限正确，能访问分配给自己的案件

### 2. 实现站内消息功能

**后端修改**：
- 创建Message模型，存储系统消息
- 创建消息控制器，处理消息的创建、获取和标记已读
- 添加消息路由，提供API端点
- 集成Socket.IO，实现实时消息推送

**前端修改**：
- 创建消息中心组件，显示所有站内消息
- 在Layout组件中添加消息通知图标和未读消息计数
- 实现消息列表和详情页面
- 集成Socket.IO，接收实时消息

### 3. 实现弹窗提醒功能

**后端修改**：
- 在消息创建时，通过Socket.IO发送实时通知
- 存储用户的通知设置

**前端修改**：
- 实现消息弹窗组件，在收到新消息时显示
- 添加通知设置，允许用户配置通知方式
- 集成Socket.IO，接收实时弹窗通知

### 4. 完善调解员留言功能

**前端修改**：
- 更新CaseQuery.tsx中的handleSubmitMessage函数，正确处理notificationType字段
- 在发送留言时，根据选择的通知方式发送相应的通知

**后端修改**：
- 更新addCaseProgress函数，处理notificationType字段
- 根据通知方式发送相应的通知

## 实现步骤

1. **创建消息模型和控制器**
2. **添加消息路由**
3. **实现Socket.IO消息推送**
4. **创建前端消息中心组件**
5. **实现消息弹窗功能**
6. **完善调解员留言功能**
7. **测试所有功能**

这样，调教员123就能看到留言，并且系统会通过站内消息和弹窗提醒的方式通知他们新的消息。